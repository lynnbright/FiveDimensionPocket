# 請再幫忙填到card~
<p><%= @tag.name %></p>
<div class="content">
  <div class="container">
    <% @tag.articles.each do |article| %>
      <p><%= article.title %> </p>
    <% end %>
  </div>
</div>